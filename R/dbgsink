debugging in: g()
debug at i.R#2: {
    y <- NULL
    nr <- nrow(a)
    nc <- ncol(a)
    for (i in 2:(nr - 1)) {
        for (j in 2:(nc - 1)) {
            y <- c(y, a[i, j])
        }
    }
}
debug at i.R#4: y <- NULL
debug at i.R#5: nr <- nrow(a)
